"""
Registry of external data sources (search engines, RSS, apis).
Keep keys here and swap to env-based tokens for production.
"""
SOURCES = {
    "ipify": "https://api.ipify.org?format=json",
    "example_api": "https://httpbin.org/get"
}
